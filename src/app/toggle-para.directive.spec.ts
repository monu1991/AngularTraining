import { ToggleParaDirective } from './toggle-para.directive';

describe('ToggleParaDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleParaDirective();
    expect(directive).toBeTruthy();
  });
});
